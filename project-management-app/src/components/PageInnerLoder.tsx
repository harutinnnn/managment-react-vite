import {Loader} from "lucide-react";

export const PageInnerLoader = () => {
    return (
        <div className="page-inner-loader">
            <Loader size={50}  className="loader-icon" />
        </div>
    )
}